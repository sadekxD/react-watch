import React from "react";
import { Range, getTrackBackground } from "react-range";

const STEP = 0.1;
const MIN = 0;
const MAX = 100;

const Duration = () => {
	return (
		<div>
			<Range />
		</div>
	);
};

export default Duration;
